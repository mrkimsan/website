<div class="bodyWrapper content">
	<div class="section postsSection">
		<h2 class="title">
			<span class="mainTitle">{{ locale.projects.title }}</span>
		</h2>
		<div class="posts">
			{{#each projects}}
			{{> post}}
			{{/each}}
		</div>
		
	</div>
</div>